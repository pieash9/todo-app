import React from 'react';
interface Props ={
    todo: Todo
}

const SingleTodo:React.FC = ({todo,todos,setTodos}) => {
    return (
        <div>
            
        </div>
    );
};

export default SingleTodo;